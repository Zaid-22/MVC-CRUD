@model MvcCrudProject.ViewModels.CityViewModel
@{
    ViewData["Title"] = "Add City";
    ViewData["PageTitle"] = "Add City";
    ViewData["PageSubtitle"] = "Create a new city entry";
}
<div class="block block-rounded">
    <div class="block-header block-header-default">
        <h3 class="block-title">Add City</h3>
    </div>
    <div class="block-content block-content-full">
        <div class="row">
            <div class="col-lg-8">
                <form asp-action="Create">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger d-none"></div>
                    <div class="mb-4">
                        <label asp-for="CityName" class="form-label">City Name</label>
                        <input asp-for="CityName" class="form-control" placeholder="Enter city name" />
                        <span asp-validation-for="CityName" class="text-danger fs-sm"></span>
                    </div>
                    <div class="mb-4">
                        <label asp-for="CountryId" class="form-label">Country</label>
                        <select asp-for="CountryId" asp-items="ViewBag.CountryId" class="form-select">
                            <option value="">-- Select Country --</option>
                        </select>
                        <span asp-validation-for="CountryId" class="text-danger fs-sm"></span>
                    </div>
                    <div class="mb-4">
                        <button type="submit" class="btn btn-primary"><i class="fa fa-save me-1"></i> Save</button>
                        <a asp-action="Index" class="btn btn-alt-secondary ms-1"><i class="fa fa-arrow-left me-1"></i> Back</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
@section Scripts { @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); } }
