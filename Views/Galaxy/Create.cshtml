@model MvcCrudProject.ViewModels.GalaxyViewModel
@{
    ViewData["Title"] = "Add Galaxy";
    ViewData["PageTitle"] = "Add Galaxy";
    ViewData["PageSubtitle"] = "Create a new galaxy entry";
}

<div class="block block-rounded">
    <div class="block-header block-header-default">
        <h3 class="block-title">Add Galaxy</h3>
    </div>
    <div class="block-content">
        <form asp-action="Create" class="row g-3">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

            <div class="col-md-6">
                <label asp-for="GalaxyName" class="form-label fw-semibold">Galaxy Name</label>
                <input asp-for="GalaxyName" class="form-control" placeholder="Enter galaxy name" />
                <span asp-validation-for="GalaxyName" class="text-danger fs-sm"></span>
            </div>

            <div class="col-12">
                <button type="submit" class="btn btn-primary">
                    <i class="fa fa-save me-1"></i> Save
                </button>
                <a asp-action="Index" class="btn btn-alt-secondary ms-1">
                    <i class="fa fa-arrow-left me-1"></i> Back to List
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
