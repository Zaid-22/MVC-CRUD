@model IEnumerable<MvcCrudProject.ViewModels.DepartmentViewModel>
@{
    ViewData["Title"] = "Departments";
    ViewData["PageTitle"] = "Departments";
    ViewData["PageSubtitle"] = "Manage all departments";
}
<div class="block block-rounded">
    <div class="block-header block-header-default">
        <h3 class="block-title">All Departments</h3>
        <div class="block-options">
            <a asp-action="Create" class="btn btn-sm btn-primary"><i class="fa fa-plus me-1"></i> Add New</a>
        </div>
    </div>
    <div class="block-content block-content-full overflow-x-auto">
        <table class="table table-bordered table-striped table-vcenter js-dataTable-full">
            <thead>
                <tr>
                    <th class="text-center" style="width:60px">#</th>
                    <th>Department Name</th>
                    <th>Branch</th>
                    <th class="text-center" style="width:160px">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="text-center fs-sm">@item.DepartmentId</td>
                        <td class="fw-semibold fs-sm">@item.DepartmentName</td>
                        <td class="fs-sm">@item.BranchName</td>
                        <td class="text-center">
                            <a asp-action="Edit" asp-route-id="@item.DepartmentId" class="btn btn-sm btn-alt-warning me-1"><i class="fa fa-pencil-alt"></i></a>
                            <a asp-action="Details" asp-route-id="@item.DepartmentId" class="btn btn-sm btn-alt-info me-1"><i class="fa fa-eye"></i></a>
                            <a asp-action="Delete" asp-route-id="@item.DepartmentId" class="btn btn-sm btn-alt-danger"><i class="fa fa-trash"></i></a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
